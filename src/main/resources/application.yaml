server:
  port: 8080
  error:
    include-message: always

spring:
  application:
    name: afsuam-mx-af00X-sa-hello-world

  datasource:
    url: jdbc:mysql://localhost:3306/inventario?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: jrmk
    password: pas1234
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect

info:
  app:
    name: ${spring.application.name}
    description: System API de Hello World referenciando DB inventario local
    version: 1.0.0
